# ── Base de Datos ESCRITURA (bonos) → Supabase ────────────────────────────────
DB_USER=postgres.pkihspbkksoggvvwcbam
DB_PASSWORD=CHANGE_ME_2026
DB_NAME=postgres
DB_HOST=aws-1-us-west-2.pooler.supabase.com
DB_PORT=5432

# ── Base de Datos SOLO LECTURA (evaluaciones) → misma Supabase ────────────────
EVAL_DB_USER=postgres.pkihspbkksoggvvwcbam
EVAL_DB_PASSWORD=CHANGE_ME_2026
EVAL_DB_NAME=postgres
EVAL_DB_HOST=aws-1-us-west-2.pooler.supabase.com
EVAL_DB_PORT=5432

# ── Redis / Celery (local en Docker) ─────────────────────────────────────────
REDIS_URL=redis://bonus_redis:6379/0
CELERY_BROKER_URL=redis://bonus_redis:6379/0
CELERY_RESULT_BACKEND=redis://bonus_redis:6379/1

# ── Seguridad JWT (Django genera el token – este servicio solo valida) ───────
# ¡NUNCA comitees el valor real! Usa Docker Secret o Vault en producción.
SECRET_KEY=super-secret-placeholder-change-me-in-production-2026
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15

# ── Cifrado en reposo (LFPDPPP Art. 18-19 + OWASP Domain 3) ─────────────────
# Generar una clave nueva en producción con este comando:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=tu_clave_fernet_real_aqui_44_caracteres_2026==

# ── Auditoría ────────────────────────────────────────────────────────────────
AUDIT_REDIS_CHANNEL=audit.events